import { InteractionFeedback } from '../../../../proto/ai/inworld/engine/v1/feedback_pb';
import { Feedback } from '../../entities/feedback.entity';
import { SessionToken } from '../../entities/session_token.entity';
export interface CreateInteractionFeedbackProps {
    scene: string;
    correlationId?: string;
    sessionToken: SessionToken;
    interactionId: string;
    interactionFeedback: InteractionFeedback;
}
export interface DeletenteractionFeedbackProps {
    sessionToken: SessionToken;
    name: string;
}
export declare class FeedbackClientGrpcService {
    private readonly config;
    private readonly address;
    private readonly ssl;
    private readonly client;
    private logger;
    createInteractionFeedback(props: CreateInteractionFeedbackProps): Promise<Feedback>;
    deleteInteractionFeedback(props: DeletenteractionFeedbackProps): Promise<void>;
    private getMetadata;
}
