import { SessionAccessToken } from '../../../proto/ai/inworld/studio/v1alpha/tokens_pb';
import { SessionTokenProps } from '../common/data_structures';
export declare class SessionToken {
    token: string;
    type: string;
    expirationTime: Date;
    sessionId: string;
    constructor(props: SessionTokenProps);
    getToken(): string;
    getType(): string;
    getExpirationTime(): Date;
    getSessionId(): string;
    static isExpired(token: SessionToken): boolean;
    static serialize(token: SessionToken): string;
    static deserialize(json: string): SessionToken;
    static fromProto(proto: SessionAccessToken): SessionToken;
}
