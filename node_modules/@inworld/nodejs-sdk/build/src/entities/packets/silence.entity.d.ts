import { DataChunk } from '../../../../proto/ai/inworld/packets/packets_pb';
export declare class SilenceEvent {
    readonly durationMs: number;
    constructor({ durationMs }: {
        durationMs: number;
    });
    static fromProto(proto: DataChunk): SilenceEvent;
}
